import { PublicPlaceholder } from "@/components/ui/PublicPlaceholder";
import { ptBR } from "@/lib/i18n";

export default function SafetyPage() {
  return <PublicPlaceholder {...ptBR.routes.safety} />;
}
